# AI测验生成器 - 测试和调试方案

## 第一阶段：修复已知错误

### 1. 解决国际化翻译缺失问题

首先需要解决控制台显示的翻译错误，特别是缺少中文翻译文件：

```
1. 创建缺失的中文翻译文件
   - 在`src/i18n/pages/quiz-generator/`目录下创建`zh.json`
   - 确保内容与`en.json`结构一致，但文本翻译为中文

2. 添加缺少的'common.back'翻译
   - 在`src/i18n/messages/zh.json`文件中添加"back": "返回"
```

### 2. 修复NextAuth配置错误

```
1. 在`.env.local`文件中添加必要的NextAuth secret
   - 添加`NEXTAUTH_SECRET=<随机安全字符串>`
   - 可使用`openssl rand -base64 32`生成安全字符串
```

### 3. 修复动态路由参数使用问题

```
1. 修复页面元数据生成函数中的params.locale使用方式
   - 确保在`src/app/[locale]/(default)/quiz-generator/page.tsx`中正确等待params
```

## 第二阶段：功能性测试执行清单

### 1. 测验生成流程测试

```
【测试案例1】：基本功能测试
- 选择"三年级"和"数学"科目
- 点击"生成测验"
- 验证：测验应在5秒内生成，包含5道数学选择题

【测试案例2】：边界情况测试
- 尝试在不选择任何年级或科目的情况下生成
- 验证：应显示适当的验证错误消息

【测试案例3】：国际化生成测试
- 切换到中文界面
- 选择"五年级"和"科学"科目
- 点击"生成测验"
- 验证：生成的测验应显示中文内容
```

### 2. 编辑功能测试

```
【测试案例4】：题目编辑测试
- 生成测验后点击"编辑"按钮
- 修改第一题的问题内容
- 验证：修改应立即保存并显示

【测试案例5】：选项编辑测试
- 编辑第二题的选项内容
- 更改正确答案选项
- 验证：更改应正确保存，新的正确答案应被标记

【测试案例6】：长文本编辑测试
- 尝试输入超长文本作为问题或选项
- 验证：界面应适当处理长文本，不会破坏布局
```

### 3. 数据持久化测试

```
【测试案例7】：刷新持久性测试
- 生成测验并进行一些编辑
- 刷新页面
- 验证：编辑后的测验内容应完整恢复

【测试案例8】：导航持久性测试
- 生成测验后导航到其他页面
- 返回测验生成器页面
- 验证：之前的测验内容应该保持不变
```

### 4. 国际化功能测试

```
【测试案例9】：UI元素翻译测试
- 在英文和中文界面间切换
- 验证：所有UI元素（按钮、标签、提示）应正确翻译

【测试案例10】：动态内容翻译测试
- 在不同语言环境下使用演示功能
- 验证：演示测验内容应随语言环境变化
```

### 5. 导出功能测试

```
【测试案例11】：打印预览测试
- 生成测验，点击导出按钮
- 选择预览选项
- 验证：应显示格式正确的打印预览

【测试案例12】：PDF导出测试
- 测试"导出为PDF"功能
- 验证：生成的PDF应包含完整测验内容和正确格式
```

### 6. 响应式设计测试

```
【测试案例13】：移动设备视图测试
- 使用Chrome开发者工具模拟不同尺寸设备
- 测试iPhone、iPad和各种Android设备尺寸
- 验证：所有元素应正确显示，无溢出或遮挡

【测试案例14】：交互适应性测试
- 在触摸屏模式下测试所有交互元素
- 验证：按钮、下拉菜单等应易于点击，不会误触
```

## 第三阶段：非功能性测试

### 1. 性能测试

```
【测试案例15】：加载性能测试
- 使用Chrome Performance面板测量首次加载性能
- 验证：首次内容绘制应在2秒内完成

【测试案例16】：API响应性能测试
- 监控测验生成API的响应时间
- 验证：API调用应在5秒内完成，或显示适当进度
```

### 2. 可用性测试

```
【测试案例17】：键盘导航测试
- 仅使用键盘完成完整测验生成和编辑流程
- 验证：所有功能应可通过键盘访问，焦点顺序合理

【测试案例18】：辅助技术测试
- 使用NVDA或VoiceOver等屏幕阅读器测试
- 验证：所有内容应能被正确朗读，包括动态生成内容
```

### 3. 稳定性测试

```
【测试案例19】：连续操作测试
- 连续生成多个测验（10次以上）
- 验证：应用应保持稳定，无性能下降

【测试案例20】：错误恢复测试
- 模拟网络故障或API错误
- 验证：应用应优雅处理错误并提供重试选项
```

## 第四阶段：调试与修复

### 1. 工具与方法

```
1. 使用浏览器开发工具
   - Chrome DevTools的Elements面板检查DOM结构
   - Console面板监控JavaScript错误
   - Network面板分析API请求
   - Performance面板识别性能瓶颈

2. React开发工具
   - 使用React Developer Tools分析组件树
   - 检查组件重渲染情况和性能问题
   - 监控Context状态变化

3. 日志增强
   - 在关键功能点添加结构化日志
   - 使用不同级别区分（info, warning, error）
   - 在生产环境禁用过多日志
```

### 2. 问题修复流程

```
1. 错误收集
   - 使用Trello或GitHub Issues跟踪发现的所有问题
   - 记录详细的复现步骤和预期行为
   
2. 优先级分类
   - P0：阻断性问题（应用无法正常使用）
   - P1：高优先级（主要功能受影响）
   - P2：中优先级（次要功能或UX问题）
   - P3：低优先级（细微问题或优化项）
   
3. 修复实施
   - 从P0问题开始，确保基本功能可用
   - 采用小步迭代修复，避免引入新问题
   - 每次修复后重新运行相关测试案例验证
```

## 第五阶段：最终验收测试

### 1. 覆盖性检查

```
1. 功能覆盖性检查
   - 确认所有12个任务的功能点已全部实现
   - 验证MVP范围内的所有需求已满足
   
2. 跨浏览器验证
   - 在Chrome、Firefox、Safari、Edge上完整测试主流程
   - 确认UI渲染和功能在各浏览器中一致
```

### 2. 用户体验验证

```
1. 完整用户旅程测试
   - 从首次访问到测验生成、编辑和导出的完整流程
   - 确认流程流畅，无阻碍点
   
2. 首次使用体验检查
   - 验证新用户引导提示是否足够清晰
   - 测试演示功能能否有效展示产品价值
```

## 实施计划

1. **首日（4小时）**:
   - 修复已发现的国际化和NextAuth错误
   - 完成功能性测试案例1-7

2. **次日（4小时）**:
   - 完成功能性测试案例8-14
   - 执行非功能性测试案例15-20
   - 收集并分类所有发现的问题

3. **第三日（4小时）**:
   - 修复P0和P1级别问题
   - 进行回归测试确认修复有效
   - 优化用户体验细节

4. **最终日（3小时）**:
   - 修复剩余P2级别问题
   - 执行最终验收测试
   - 准备发布文档和用户指南更新 